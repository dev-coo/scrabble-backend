# 스크래블 백엔드 의존성
#
# 설치:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt

fastapi
uvicorn[standard]

# PostgreSQL 드라이버. [binary] 는 컴파일 없이 바로 설치되는 버전입니다.
psycopg[binary]

# 에러 추적. 서버에서 터진 오류를 Sentry 로 보냅니다.
#
# [fastapi] 는 FastAPI 에 맞는 연결 부품을 함께 깝니다. 이게 있어야
# "어느 주소에서 터졌는지"까지 함께 올라갑니다.
#
# ⚠️ SENTRY_DSN 환경변수가 없으면 **아무것도 보내지 않습니다.**
#    그래서 이걸 깔아도 그냥 조용합니다. (sentry.py 참고)
sentry-sdk[fastapi]

# 웹소켓 명세(docs/asyncapi.yaml)를 읽는 데 씁니다.
# uvicorn[standard] 를 깔면 딸려 오긴 하지만, 우리가 직접 쓰는 것이므로
# 여기 적어 둡니다. 안 적어 두면 uvicorn 쪽 사정으로 사라졌을 때
# 이유를 모른 채 서버가 안 뜹니다.
PyYAML
